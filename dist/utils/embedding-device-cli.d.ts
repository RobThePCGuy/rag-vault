/**
 * Apply CLI overrides for embedding device and return remaining args.
 *
 * Supported flags:
 * - --embedding-device <value>
 * - --embedding-device=<value>
 * - --device <value>
 * - --device=<value>
 * - --gpu-auto
 */
export declare function applyEmbeddingDeviceCliOverride(args: string[]): string[];
//# sourceMappingURL=embedding-device-cli.d.ts.map